[pytest]
# Make project packages importable without sys.path hacks in test files.
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    network: tests that hit live external APIs (deselect with -m "not network")
